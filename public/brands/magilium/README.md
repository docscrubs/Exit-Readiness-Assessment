Magilium brand assets.

- logo.svg (TODO: replace the builder.io CDN URL currently used in src/app/app.html with a local SVG)
- favicons/ (copied from the public/ root — originals will be removed once per-host index.html variants land in Phase 3)
